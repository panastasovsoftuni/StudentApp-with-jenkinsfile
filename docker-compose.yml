version: '3'
services:
  app:
    image: panastasovsoftuni/softuni-stud-jenkins
    ports:
      - "18081:3030" # Map port 3030 on the host to port 3030 in the container
    environment:
      - NODE_ENV=production
